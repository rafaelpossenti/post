spring:
  banner:
    location=classpath: banner.txt
  application:
    name: post
  profiles:
    active: development
  cloud:
    config:
      fail-fast: true
      uri:
        - http://localhost:8888
